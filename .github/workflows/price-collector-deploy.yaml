name: Build & Deploy Price Collector

on:
  push:
    branches:
      - 'feature/**'
    paths:
      - 'apps/price-collector/**'
      - '.github/workflows/price-collector-ci.yml'

jobs:
  build-and-deploy-price-collector:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build BootJar
        run: ./gradlew :apps:price-collector:bootJar -x test

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image
        run: |
          docker build \
            -t yeonwoo02/skytracker-price-collector:latest \
            -f apps/price-collector/Dockerfile .

      - name: Push Image
        run: docker push yeonwoo02/skytracker-price-collector:latest